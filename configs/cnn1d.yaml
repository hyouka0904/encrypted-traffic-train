model:
  name: cnn1d
  params:
    channels: [32, 64, 128]
    kernel_size: 5
    dropout: 0.3
    hidden_dim: 128
  train_params:
    epochs: 50
    lr: 0.001
    batch_size: 256

data:
  processed_dir: data/processed

output:
  dir: models